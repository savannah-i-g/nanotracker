# Build trees (any configuration: build/, build-asan/, ...).
build*/

# Vendored libopenmpt working tree (regenerated by
# tools/build_libopenmpt.sh; the pin lives in Docs/DEPENDENCIES.md).
third_party/

# Tooling droppings.
.cache/
compile_commands.json
*.swp
*~
